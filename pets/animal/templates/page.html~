<div id="wall" class="row-fluid">
{% for i in animals %}
<div class="element span3 item ">
    <div class="picture">
        <a class="image" href="{% url 'animal:profile' i.id %}" title="{{i.name}}"><img src="/static/media/{{i.smal_img_file}}" alt=""/></a>
       
	<div class="description">
123
            <p>{{i.note}}
            </p>
        </div>
        <div class="meta">
            <span><i class="fa-icon-heart-empty"></i> <a href="{% url 'animal:profile' i.id %}">{{i.name}}</a></span>
            <span><i class="fa-icon-phone">{{i.phone}}</i></span>
            <br />
            <span><i class="fa-icon-home muted"><small>{{i.resettlement}}</small></i></span>
        </div>
    </div>
</div>
{% endfor %}
</div>
